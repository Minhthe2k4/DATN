spring.application.name=DATN
# Database connection
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3305/vocab_learning?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Minhthe2004:)}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Use manual SQL migrations; avoid Hibernate auto-DDL to prevent FK conflicts.
spring.jpa.hibernate.ddl-auto=none

server.port=8080

# Return concise API errors without stack traces.
server.error.include-stacktrace=never
server.error.include-message=always

# Optional: enable AI condensation + context meaning selection in reading dictionary lookup.
datn.ai.openai-api-key=${OPENAI_API_KEY:your-key-here}
datn.ai.openai-model=gpt-4o-mini

# Dictionary provider for reading lookup (freedictionary or oxford)
# (DEPRECATED: now using AI exclusively for reading)

# ZaloPay Sandbox configuration
zalopay.app-id=2554
zalopay.key1=sdngKKJmqEMzvh5QQcdD2A9XBSKUNaYn
zalopay.key2=trMrHtvjo6myautxDUiAcYsVtaeQ8nhf
zalopay.endpoint=https://sb-openapi.zalopay.vn/v2/create
zalopay.redirect-url=${ZALOPAY_REDIRECT_URL:http://localhost:5173/payment-result}

# Frontend URL for CORS
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}


# ========================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dfio1duaf}
cloudinary.api-key=${CLOUDINARY_API_KEY:652225234411389}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:ieY0vRLVjr3PdyD-tXP6kEwd3fU}

# Giới hạn kích thước file upload gửi lên server (trước khi forward lên Cloudinary)
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# Đường dẫn công cụ (Tool paths)
# Trên production (Docker), các công cụ này sẽ nằm trong system path
datn.tool.yt-dlp=${YT_DLP_PATH:yt-dlp}
datn.tool.ffmpeg=${FFMPEG_PATH:ffmpeg}

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenminhthe2hk4@gmail.com
spring.mail.password=derv mkle tfcc femr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=true

# Google OAuth2 Configuration
datn.google.client-id=611258885638-83p3ulhfr9cra47glbp858hqtdf6p6l5.apps.googleusercontent.com

