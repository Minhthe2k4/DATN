# Cấu hình dành riêng cho môi trường Deploy (Render)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

datn.ai.openai-api-key=${OPENAI_API_KEY}
app.frontend.url=${APP_FRONTEND_URL}
zalopay.redirect-url=${ZALOPAY_REDIRECT_URL}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# Tool paths in Linux (Docker)
datn.tool.yt-dlp=/usr/local/bin/yt-dlp
datn.tool.ffmpeg=/usr/bin/ffmpeg
server.port=${PORT:10000}
server.address=0.0.0.0

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.devtools.restart.enabled=false
spring.main.banner-mode=off
logging.level.org.springframework.boot=INFO
logging.level.com.example.DATN=DEBUG

spring.jpa.open-in-view=false
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.validation-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# Email Config (Production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:nguyenminhthe2hk4@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:derv mkle tfcc femr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Google OAuth2 (Production)
datn.google.client-id=${GOOGLE_CLIENT_ID:611258885638-83p3ulhfr9cra47glbp858hqtdf6p6l5.apps.googleusercontent.com}

# ZaloPay (Production)
zalopay.app-id=${ZALOPAY_APP_ID:2554}
zalopay.key1=${ZALOPAY_KEY1:sdngKKJmqEMzvh5QQcdD2A9XBSKUNaYn}
zalopay.key2=${ZALOPAY_KEY2:trMrHtvjo6myautxDUiAcYsVtaeQ8nhf}
zalopay.endpoint=${ZALOPAY_ENDPOINT:https://sb-openapi.zalopay.vn/v2/create}

# OpenAI (Production)
datn.ai.openai-model=${OPENAI_MODEL:gpt-4o-mini}
